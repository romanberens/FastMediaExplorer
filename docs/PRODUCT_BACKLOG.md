# Product Backlog

## Duplicate Analysis

1. Dedicated duplicate groups view.
2. Duplicate policy engine (`Newest`, `Oldest`, `ShortestPath`, `LongestPath`, `FirstSeen`).
3. Batch actions with recycle-bin safety.
4. Export duplicate report (`CSV`, `JSON`).

## Indexing and Performance

1. Persistent index cache (`index.bin`).
2. Progressive index load.
3. Memory limits and diagnostics.
4. Smarter thumbnail prefetch.

## Analytics and Insight

1. Timeline/statistics panel (`files by month/day`).
2. Largest files analytics (`Top 100`).
3. Metadata extraction (`EXIF`, resolution, camera).

## UX Polish

1. Multi-select improvements.
2. Drag & drop polish.
3. Keyboard navigation polish.
4. Status bar statistics expansion.
5. Quick toolbar.

## Long-term Platform

1. Plugin system (`plugins/*.dll`).
2. Analyzer/filter registration API.
3. Watch mode (`ReadDirectoryChangesW`).
4. Similarity duplicates (`pHash`, `aHash`, `dHash`).
